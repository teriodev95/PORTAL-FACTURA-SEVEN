{
  "name": "seven-days-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply seven-days-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply seven-days-db --remote"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "drizzle-orm": "^0.39.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250313.0",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
